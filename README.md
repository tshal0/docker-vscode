# Docker VSCode Template

Instructions: 

1. Clone repo to local folder
2. Open in VS Code
3. Run Remote Containers > Open Folder in Container

Terminal in container will be in bash (so normal terminal text selection shortcuts wont work...)
